@model IEnumerable<VM_SubCategory_List>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("opret ny", "SubCategory_Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Category.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Category.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.ActionLink("ret", "SubCategory_Edit", new { id = item.SubCategoryId }) |
                @Html.ActionLink("Details", "SubCategory_Details", new { id = item.SubCategoryId }) |
                @Html.ActionLink("slet", "SubCategory_Delete", new { id = item.SubCategoryId })
            </td>
        </tr>
    }

</table>
