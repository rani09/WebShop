@model IEnumerable<VM_Size_List>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Opret ny", "Size_Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("ret", "Size_Edit", new { id=item.SizeId }) |
            @Html.ActionLink("Details", "Size_Details", new { id=item.SizeId }) |
            @Html.ActionLink("slet", "Size_Delete", new { id=item.SizeId })
        </td>
    </tr>
}

</table>
